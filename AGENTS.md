## Agent 技能

### Issue 跟踪器

创建、读取或更新需求与规格时，使用 GitHub Issues。详见 `docs/agents/issue-tracker.md`。

### Triage 标签

评估或流转 Issue 时，使用五种默认的规范 Triage 标签。详见 `docs/agents/triage-labels.md`。

### 领域文档

探索业务术语或架构决策前，按 single-context 布局读取领域文档。详见 `docs/agents/domain.md`。
